package constants;

/**
 * Represents the constants screen width, screen height, and brick size (the size of an in-game "block"). 
 */
public interface Constants {

	final int SCREEN_WIDTH = 800;
	final int SCREEN_HEIGHT = (int) (800/1.25);
	final double BRICK_SIZE = SCREEN_WIDTH/20;
	
}
